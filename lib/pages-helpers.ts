/**
 * @deprecated Re-exports retained only for backward compatibility. Import
 * directly from `@/lib/pages` and `@/lib/seo` in new code.
 */
export { getPageBySlug, pageHref } from "./pages";
export { jsonLdFor as jsonLdHelper, jsonLdFor } from "./seo";
